{
  "name": "kallipai",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "sync": "deno task --filter @kallipai/kallip-web prepare",
    "dev": "deno task --filter @kallipai/kallip-web dev",
    "build": "deno task --filter @kallipai/kallip-web build",
    "clean": "deno task --filter @kallipai/kallip-web clean && deno task --filter @kallipai/kallip-app clean",
    "check": "deno task --filter @kallipai/kallip-common check && deno task --filter @kallipai/kallip-client check && deno task --filter @kallipai/kallip-agora-client check && deno task --filter @kallipai/kallip-lesche-client check && deno task --filter @kallipai/kallip-ui check && deno task --filter @kallipai/kallip-web check && deno task --filter @kallipai/kallip-app check",
    "test": "deno task --filter @kallipai/kallip-common test && deno task --filter @kallipai/kallip-client test && deno task --filter @kallipai/kallip-agora-client test && deno task --filter @kallipai/kallip-lesche-client test",
    "fmt": "prettier --write . && rumdl fmt .",
    "fmt:file": "prettier --write",
    "fmt:md": "rumdl fmt",
    "fmt:check": "prettier --check . && rumdl fmt --check .",
    "lint": "deno lint"
  },
  "devDependencies": {
    "prettier": "^3.4.0",
    "prettier-plugin-svelte": "^4.1.1",
    "prettier-plugin-tailwindcss": "^0.6.0"
  },
  "engines": {
    "deno": ">=2.9.2"
  }
}
