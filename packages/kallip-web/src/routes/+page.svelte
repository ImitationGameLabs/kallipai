<script lang="ts">
  // The root `/` is never rendered: the gate redirects offline users to
  // /local and online users to /tagmata before this page mounts. The
  // route exists so SvelteKit has a page for the layout to wrap during the
  // (skeleton) resolving window.
</script>

<div class="p-4"></div>
