// @kallipai/kallip-agora-client
//
// E2EE agora relay client: HTTP + SSE over the agora /v1 surface plus the
// crypto the app side performs (X25519 + HKDF, ChaCha20-Poly1305, Ed25519
// verify). See the frontend plan, section "packages/agora-client".

export const PACKAGE_NAME = "@kallipai/kallip-agora-client";
