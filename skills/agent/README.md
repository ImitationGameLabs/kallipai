---
description: Agent self-management — the kallip CLI, subagents, and coordination with the tagma. Look here for anything about managing your own runtime (skills live in `skill/`).
---

# Agent skills

Skills for managing your own runtime — the kallip CLI, subagents, and tagma
coordination. Skill authoring and management live in `skill/`. The entries here
are generated on demand — run `kallip skill index <skills-path>/agent` for the
live list.
